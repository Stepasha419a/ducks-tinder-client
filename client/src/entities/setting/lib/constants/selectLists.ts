import type { ProfileSettingName } from '../../model';
import { SETTING_INTERESTS_LIST } from '../../model/setting.constants';

export const SELECT_LISTS: Record<ProfileSettingName, string[]> = {
  interests: SETTING_INTERESTS_LIST,
};
