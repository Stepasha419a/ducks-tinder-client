name: 'ducks-tinder-client'
services:
  nginx:
    container_name: nginx
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./cert/dev-docker:/etc/nginx/cert
    env_file:
      - .env.dev
    ports:
      - '80:80'
      - '443:443'
